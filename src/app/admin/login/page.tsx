import React from 'react';
import AdminLoginContent from './components/AdminLoginContent';

export const metadata = {
  title: "Admin Login — Daddee\'s Shelf",
};

export default function AdminLoginPage() {
  return <AdminLoginContent />;
}
