// Re-export from inventoryData for backward compatibility
// The official inventory is now in src/lib/inventoryData.ts
export { INVENTORY_BOOKS, MOCK_BOOKS, GENRES, FORMATS, MOCK_BUNDLES } from './inventoryData';

export const BATCHES: any = null;